# Fitrater — App Store Submission Readiness

*Scope: the iOS App Store target (`ios/FitScore`, bundle `com.fitrater.app`). Android findings are called out only where they affect iOS parity. Every claim below was verified against the working tree at `/Users/efe/clothes31` (branch `fix/app-store-3-2-2-and-aasa`, HEAD `6dd7e4e`).*

## Verdict

**No — this cannot be submitted today, and not only for policy reasons: it cannot even be uploaded correctly.** `ios/FitScore/FitScore/Info.plist:13` hardcodes `CFBundleVersion` to `4` while `GENERATE_INFOPLIST_FILE = NO` (`project.pbxproj:670`, `:700`), so the `CURRENT_PROJECT_VERSION = 15` bumps (`project.pbxproj:669`, `:699`) never reach the binary — every archive ships as build 4. Behind that sit three genuine rejection-grade defects: the reviewer demo account password is still the literal placeholder `(fill in before submission)` on a 100%-auth-gated app that was *already* rejected under 2.1(a); "Delete my account" wipes some rows and signs out without ever deleting the `auth.users` record (5.1.1(v)), while `REVIEW_NOTES.md:59` tells the reviewer it "Deletes the auth user"; and the App Privacy answers you plan to paste into ASC swear the app contains no Firebase while FirebaseAnalytics + Crashlytics are linked and `FirebaseApp.configure()` runs at launch. The single biggest thing standing in the way is that **the reviewer-facing documents (`REVIEW_NOTES.md`, `APP_STORE_METADATA.md`, `APP_PRIVACY_ANSWERS.md`) describe an app that does not exist** — they promise brutal-mode critiques, a nudity moderation pass, Restore in Settings, and account deletion, none of which are in the build. Realistic time to submittable: 1–2 days of focused work.

---

## Blockers — will be rejected

| Guideline | Issue | Evidence (file:line) | Fix | Effort |
|---|---|---|---|---|
| ITMS upload (not review) | **CFBundleVersion is hardcoded to `4`**; pbxproj bumps to 15 never reach the binary. Verified: built products in DerivedData (`Debug-iphoneos/Fitrater.app/Info.plist`) carry `4`. Any upload is a duplicate/lower bundle version (ITMS-4238). | `ios/FitScore/FitScore/Info.plist:13` (`<string>4</string>`); `ios/FitScore/FitScore.xcodeproj/project.pbxproj:669-670`, `:699-700` (`CURRENT_PROJECT_VERSION = 15; GENERATE_INFOPLIST_FILE = NO`). No `.xcconfig`, agvtool, or version logic in `ios/store/submit.sh`. Already logged at `docs/PARITY_FIX_PLAN.md:60`, fix at `:251`. | Change Info.plist:12-13 to `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)`; add a post-archive `plutil -p` assertion in `submit.sh`. | minutes |
| 2.1(a) | **Demo account password is a literal placeholder and the account was never created.** App is fully auth-gated with no guest path; this is the exact axis of the build-12 rejection. | `ios/store/REVIEW_NOTES.md:14` "Password: (fill in before submission)"; `:18-21` account creation is a manual to-do; `ios/store/APP_STORE_SUBMISSION_CHECKLIST.md:90-91` both boxes unchecked; `ios/FitScore/FitScore/App/RootView.swift:12-33` no guest stage; `ios/store/SESSION_2026-08-08.md:16` prior 2.1(a) rejection. Password sign-in path itself works (`SignIn/SignInView.swift:93-109`). | Create `applereview+demo@fitrater.ai` in Supabase **with an explicit password** (in-app signup is magic-link only — `Auth/EmailAuth.swift:10-21` has no `signUp`), grant Pro + credits, verify on a clean install, paste real credentials into ASC. | minutes (ops) |
| 5.1.1(v) | **"Delete my account" never deletes the account.** It deletes 6 tables + `profiles` + 4 storage buckets, then `auth.signOut()`. The `auth.users` row survives, so the same Apple/Google/email identity signs straight back in. No SIWA token revocation exists either. Review notes assert the opposite. | `ios/FitScore/FitScore/Data/Repo.swift:1335-1357` (ends `try? await auth.signOut()`); `You/HelpPrivacySheet.swift:108-109` (`try?` then unconditional toast "Account deleted"); no deletion edge function (14 functions, none); no `trigger` in `supabase/migrations/*.sql`; `ios/store/REVIEW_NOTES.md:59` "Deletes the auth user… No server-side retention" — **false**. Residual tables: `body_profiles` (`0001:2`), `invitation_reads` (`0004:3`), cover-template rows with `outfit_id IS NULL` (`0003:3`), and the public `magazine_covers` storage bucket (`compose-cover/index.ts:1598`). | Add `supabase/functions/delete-account` calling `auth.admin.deleteUser(uid)` with the service-role key + Apple token revocation; call it from `Repo.deleteAllUserData()` before `signOut()`; explicitly `storage.remove` the `magazine_covers` prefix; stop swallowing errors with `try?`. | hours |
| 5.1.1 / 2.3 | **App Privacy answers deny Firebase; Firebase Analytics + Crashlytics are linked and initialized.** The doc is explicitly the script for the ASC App Privacy form. `IS_ANALYTICS_ENABLED=false` in GoogleService-Info.plist is not the collection switch on firebase-ios-sdk 11.x (`FIREBASE_ANALYTICS_COLLECTION_ENABLED` appears nowhere in `ios/`). | `ios/store/APP_PRIVACY_ANSWERS.md:5` "no… Firebase… in the codebase", `:62` crash data "Apple's built-in… only", `:81`, `:89-93` SDK table omits Google. Contradicted by `project.pbxproj:191-193`, `:444-446`, `:519` (Crashlytics dSYM phase), `:831/836/841`; `App/FitraterApp.swift:17` `FirebaseApp.configure()`; `Package.resolved:32-57` firebase 11.15.0 + googleappmeasurement 11.15.0. | Either remove FirebaseAnalytics/Crashlytics + `FirebaseApp.configure()` + GoogleService-Info.plist, **or** declare Identifiers→Device ID, Usage Data→Product Interaction, Diagnostics→Crash Data (third-party: Google) and add Google to the SDK table and `web/app/privacy/page.tsx` §5. Note: IDFA answer "No" (`APP_STORE_SUBMISSION_CHECKLIST.md:104`) is **correct** — no AdSupport/ATT usage anywhere. | hours |

---

## Likely rejections / high risk

| Guideline | Issue | Evidence (file:line) | Fix | Effort |
|---|---|---|---|---|
| 1.5 / 2.3 | **Support URL 404s.** Live-verified this session: `/support` → 404, `/privacy` → 200, `/terms` → 200. No route, no redirect, no middleware. | `ios/store/APP_STORE_METADATA.md:66`; self-flag at `:70`; `ls web/app` → no `support/`; `web/next.config.ts` has no `redirects()`; no `vercel.json`/`middleware.ts`. | Add `web/app/support/page.tsx` and deploy, or point the ASC Support URL at `https://fitrater.ai` (already 200). | minutes |
| 2.3.1 / 3.1.2 | **Store copy and review notes sell "brutal-mode critiques" — the feature is gone.** `REVIEW_NOTES.md:15` literally instructs the reviewer to test it. Server coerces every request to `honest`. | `APP_STORE_METADATA.md:54`; `APP_STORE_IN_APP_PURCHASES.md:35`, `:41`; `REVIEW_NOTES.md:15`. Removed in app: `You/YouSheet.swift:41` (+`honestySection` at `:178` has zero callers), `Camera/CameraMenuSheet.swift:87` (Roast tile → "Chat with Hem"), `You/CreditsSheet.swift:83`. Server: `supabase/functions/score-outfit/index.ts:23-26` `resolveHonesty` returns `"honest"` unconditionally. In-app Pro list already dropped it (`ManageProView.swift:47-51`). | Delete the phrase from all four metadata files and from the ASC description + subscription descriptions. | minutes |
| 2.3.7 | **Keyword `vogue`** — a Condé Nast mark, aggravated by in-app copy naming the brand. | `APP_STORE_METADATA.md:61`; `Studio/CoverTemplateCreatorView.swift:6, :17, :64, :474` ("real Vogue covers"). | Remove `vogue`; substitute `wardrobe` or `magazine`. | minutes |
| 5.2.1 / 5.2.5 | **Screenshot slot 2 reproduces a British Vogue cover**, verbatim cover lines plus two living public figures' names, masthead swapped to FITRATER, over an identifiable woman. | `ios/store/screenshots/2_studio.png` (1290×2796) — legible on upscale: "MOVING HOUSE / FASHION'S BIG YEAR OF CHANGE", "PAUL MESCA[L] HEATS U[P] STREETCA[STING]", "CHIMAMANDA [N]GOZI ADICHIE'[S] LOVE STOR[Y]"; queued at `APP_STORE_SUBMISSION_CHECKLIST.md:52`. | Delete and re-shoot with Fitrater-generated chrome (Branch B) and invented cover lines. | hours |
| 2.3 | **Review notes contain four statements that are false about the shipping build**, each independently testable by a reviewer. | `REVIEW_NOTES.md:64` "a moderation pass rejects such uploads" — **no upload moderation exists**: `tryon-outfit/index.ts` and `analyze-body/index.ts` return zero hits for moderat/nsfw/flag; only post-hoc provider flagging in `compose-cover/index.ts:1512`, `:1702`, with `safety_tolerance: "5"` (most permissive) at `:730`. `:47` "Restore Purchases is on the paywall **and in Settings**" — the only `Restore` string in the whole iOS tree is `CreditsSheet.swift:295`. `:35` cites RC key `appl_CzEA…` which `SESSION_2026-08-03.md:40-46` identifies as another project's; shipping key is `appl_XcRn…` (`Data/SupaClient.swift:30`). `:43-45` lists `fitrater_pro_credits_*` SKUs at $2.99/$9.99/$29.99; code uses `fitrater_v2_credits_{100,500,1500,5000}` (`CreditsSheet.swift:33-37`). | Rewrite `REVIEW_NOTES.md` end to end against the current build before pasting into ASC. | minutes |
| 2.3 / age rating | **Age-rating answers predate Stylist Chat and body analysis**, and "12+" no longer exists as a tier. Metadata says UGC = No while the app ingests user photos into a chatbot; app terms set a 16+ floor with no in-app age gate. | `APP_STORE_METADATA.md:81-87`; `APP_STORE_SUBMISSION_CHECKLIST.md:75` ("All 'None'"); `REVIEW_NOTES.md:64` "Rated 12+"; `web/app/terms/page.tsx:21-22` "at least 16"; chat at `App/MainTabView.swift:278`, body analysis at `Onboarding/BodyCalibrationView.swift:224`. | Re-run the questionnaire against build 15 disclosing the AI chatbot and AI-generated imagery; pick 13+ or 16+ and align `web/app/terms`. | minutes |
| 3.1.2 | **Monthly plan's 3-day free trial is not disclosed on the paywall**, and monthly is the emphasized default. Three other surfaces advertise "7-day trial" as if universal. | `You/CreditsSheet.swift:187-189` (monthly line omits trial) vs `:184-186` (annual has it); `:159` monthly tile `popular: true`; trial is real per `Fitrater.storekit:104-108` (`P3D`) and `APP_STORE_IN_APP_PURCHASES.md:55`. Compounding: `Camera/CameraMenuSheet.swift:63`, `You/subpages/WeeklyLetterView.swift:53`, `You/subpages/FaqView.swift:7`. | Derive disclosure from `storeProduct.introductoryDiscount` per package instead of hardcoding annual-only text. | minutes |
| 2.3.3 | **Screenshots predate Studio 2.0 and Stylist Chat.** All six PNGs dated Aug 1; the redesign landed in `d903268`. No screenshot exists for the chat surface, and the description does not mention it. | `ls -la ios/store/screenshots/`; `APP_STORE_SUBMISSION_CHECKLIST.md:57-63`. Also: all six are 2× upscales of ~650px captures (`screenshots/original/` are 632–654px wide); `5_generate.png` and `6_tryon.png` are byte-identical; `3_cover.png` actually shows the power-ups sheet. | Re-capture natively at 1290×2796 from build 15. | hours |
| 2.1 | **Stylist Chat fabricates AI replies on any backend failure.** The catch returns canned text instead of rethrowing, so `send` can never throw and the (correct) error UI at `:211-218` is dead code. No `#if DEBUG` guard — ships in Release. | `App/MainTabView.swift:258-262` (`return canned(for: last)`), `:265-274` (hardcoded wedding/linen-suit reply), doc comment at `:234-236`. **UNVERIFIED —** whether `stylist-chat` is deployed: `mcp__supabase__list_edge_functions` returns `Unauthorized`; `SESSION_2026-08-08.md:249-268` lists the deploy as pending but predates two build bumps. | Wrap the fallback in `#if DEBUG`; surface the real error with a Retry. Confirm `stylist-chat` and `compose-cover` are deployed before archiving. | hours |
| 2.1 | **Suggestion chips under AI replies look tappable but are inert `Text` views** — no Button, no gesture, no action. | `App/MainTabView.swift:453-466` (capsule + stroke + card background, zero interaction). | Wire each chip or remove them. | hours |
| 1.1 / 1.2 | **Face-swap accepts any two library photos with no consent gate**, and the backend does a dedicated identity transplant. The only rule lives in website terms, never shown at upload. | `Camera/MagazineCoverSheet.swift:289`, `:318` (unrestricted `PhotosPicker`), `:300` placeholder "Magazine cover to mimic"; `compose-cover/index.ts:13-14`, `:39`, `:361-372` ("The new person MUST look identical… Do NOT keep the original person's face"); `web/app/terms/page.tsx:69-70`. | One-time persisted consent confirmation before first cover generation + inline caption under the subject tile. | hours |

---

## Worth fixing before submission (lower risk)

- **Push entitlement with no push code.** `Fitrater.entitlements` declares `aps-environment: development`; zero `UNUserNotificationCenter`/`registerForRemoteNotifications` in `ios/FitScore/FitScore`. A `development` value in an App Store build is a distribution-signing hazard. Delete it for 1.0.
- **Release config pins `CODE_SIGN_IDENTITY = "Apple Development"`** (`project.pbxproj:698`) alongside `CODE_SIGN_STYLE = Automatic` (`:695`), and `ENABLE_PREVIEWS = YES` (`:715`). Remove both from Release.
- **Universal links claim every path.** AASA serves `paths: ["*"]` (`web/app/.well-known/apple-app-site-association/route.ts`) while `App/FitraterApp.swift:34-45` force-feeds every incoming URL to `auth.session(from:)` and toasts "Couldn't finish sign-in." A reviewer tapping a `fitrater.ai/privacy` link from Mail lands in the app on a dead end.
- **Weather is hardcoded to Istanbul for every user.** `Util/Weather.swift:8` (`latitude=41.01&longitude=28.98`), consumed at `Home/HomeView.swift:269`. Also contradicts `web/app/privacy/page.tsx:48-53`, which claims the app reads approximate location — no `CLLocation` code exists anywhere.
- **Three contact identities on one screen.** `You/HelpPrivacySheet.swift:38-39` `efe@cloudgeng.com`, `:84` `support@fitrater.com` (wrong TLD; domain is fitrater.**ai**), `web/app/privacy/page.tsx:10` `efe@cloudgeng.com`. Pick one and verify the mailbox.
- **Purchase failures are silent.** `You/CreditsSheet.swift:283-285` writes the error to `debugMsg`, which is never rendered (assigned 10×, displayed 0×). Post a toast; delete `debugMsg`.
- **`ManageProView` checks the wrong entitlement id** — `info?.entitlements.all["pro"]` (`ManageProView.swift:11`) vs the real `fitscore_pro` (`Data/RcBilling.swift:18`). Real subscribers see the upsell and never get the renewal date or the manage-subscription link. One-word fix.
- **No Terms/Privacy on the sign-in screen.** `SignIn/SignInView.swift` has zero matches for privacy/terms; the very next step uploads a full-body photo to third-party AI (`Onboarding/BodyCalibrationView.swift:224`).
- **`SKStoreReviewController.requestReview` fired from a tapped button** (`You/YouSheet.swift:284-311`) — Apple's docs forbid this and the system throttles it, so the row often does nothing. Use `?action=write-review`. (The 3.2.2(x) *incentive* is genuinely gone.)
- **No in-app content-report affordance** on any iOS AI surface (grep for `content_reports|ReportContent` in `ios/` → zero hits) while the backend table and Android UI exist. Not a hard 1.2 requirement (no user-to-user surface) but cheap insurance for a generative-AI reviewer.
- **Third-party brand names in shipping UI copy** — `Studio/CoverTemplateCreatorView.swift:6-13, :17, :39, :64, :474` render "Vogue-adjacent", "Off-White / Hypebeast", "Rolling Stone / The Face", "Kinfolk / Cereal" as visible option subtitles (rendered at `:360`, `:391`). Also `create-cover-template/index.ts:289` "Vogue-style cover photography" (server-side only).
- **User-typed cover text is cliché-filtered, not profanity-filtered.** `compose-cover/index.ts:190-196` is an editorial blocklist; `custom_headline`/`cover_lines` flow through at `:1408`, `:1421`, `:1656`.
- **Dead code:** `Onboarding/HemIntroView.swift:18` renders the literal string `"TODO: HemIntro wiring (currently unused)"`; `Camera/RoastView.swift` ("BRUTAL MODE", `honesty: "brutal"`) still compiles, reachable only via the orphan `case .roast` at `App/MainTabView.swift:66` — **verified unreachable**: `grep -rn "request(.roast)" ios/` returns nothing. Android already deleted its equivalent.
- **Unused `transcribeIntent`** (`Data/Repo.swift:709`) targets Fal wizper with no `NSMicrophoneUsageDescription` in Info.plist and Audio Data declared "not collected". Dead today; delete it.
- **No `PrivacyInfo.xcprivacy`** anywhere (`find . -iname "*xcprivacy*"` → empty). Not a blocker — first-party code uses zero required-reason APIs (no `UserDefaults`/`@AppStorage`/`systemUptime`/file-timestamp/disk-space hits), dependencies ship their own manifests, and builds 14/15 were already accepted by ASC. Add one as hardening.
- **Secrets hygiene (not an Apple issue).** Untracked-but-not-ignored `/Users/efe/clothes31/file` contains a live Supabase PAT (`sbp_1a31…`) — one `git add -A` commits it. `android/store/RELEASE_KEYSTORE_INFO.md` keystore passwords are already in git history; `.gitignore` cannot untrack them. Revoke the PAT, rotate the keystore passwords, `git rm --cached` the `.bak` and `supabase/.temp/` files.
- **Web pages that a reviewer can reach from the paywall:** `web/app/paywall/page.tsx:43-56` quotes $9.99/$59.99 vs the shipped $14.99/$79.99, has a dead Restore button (`:65-67`) and says "FitScore Pro" (`:21`); `web/app/terms/page.tsx:28` still advertises "roasts"; `:47-49` still says credits are "earned through in-app rewards" after the 3.2.2(x) removal.
- **Migration `0006_content_reports.sql` is untracked and possibly unapplied** — `git add` it and push. **UNVERIFIED —** applied state; Supabase MCP returns `Unauthorized`.
- **Doc drift:** `Info.plist:27-29` comment names an unrelated bundle id `com.cloudgeng.mealmind`; `APP_STORE_SUBMISSION_CHECKLIST.md:3` says version 1.0.0 vs `MARKETING_VERSION = 1.0`; `:30` instructs bumping `CFBundleVersion` in Info.plist, contradicting the pbxproj-based workflow — this contradiction is precisely what produced the build-number blocker.

---

## Already compliant

- **4.8 Sign in with Apple** is implemented as the primary option above Google, with proper scopes and a SHA-256 nonce — `SignIn/SignInView.swift:38-56`, entitlement `com.apple.developer.applesignin` in `Fitrater.entitlements`, wired into both configs at `project.pbxproj:666`, `:696`. The build-12 2.1(a) auth fix (`fcc615a`) is still in place at `Auth/AppleAuth.swift:30-54`.
- **3.2.2(x) rating incentive is genuinely fixed.** `You/YouSheet.swift:284-311` grants nothing; `grep -rn "playRatingReward|rated_ok" ios/` → zero matches.
- **3.1.1 anti-steering is clean.** No Stripe/checkout/external purchase links anywhere in `ios/FitScore/FitScore/`; all AI calls route through `supabase/functions/*`, no provider secrets in the bundle (`Data/SupaClient.swift:16` anon key is RLS-protected by design).
- **3.1.2 core disclosures are on the purchase screen**: Terms of Use + Privacy Policy links (`You/CreditsSheet.swift:197`, `:201`), auto-renewal and cancellation copy (`:190`), localized prices from `storeProduct.localizedPriceString` (`:224-225`), Restore Purchases (`:288-305` → `Data/RcBilling.swift:125`).
- **IAP plumbing is sound**: one subscription group with one shared entitlement (`Fitrater.storekit:61-148`, `REVENUECAT_SETUP.md:44-46`); credit grants are idempotent on the StoreKit transaction id (`Data/RcBilling.swift:135-152`); credits never expire; exact-match product-id lookup (`CreditsSheet.swift:216`) fixes the `_500`/`_5000` prefix collision.
- **AASA is live and correct** — `curl https://fitrater.ai/.well-known/apple-app-site-association` → 200, `application/json`, appID `NCYFB69WBK.com.fitrater.app`, matching `DEVELOPMENT_TEAM` (`project.pbxproj:667`) and bundle id (`:679`). Privacy and Terms both return 200.
- **2.5 export compliance declared** — `Info.plist:54-55` `ITSAppUsesNonExemptEncryption` = false. No ASC prompt at upload.
- **Purpose strings** are present and feature-specific for camera, photo library, and photo-library-add (`Info.plist:21-26`). No location/mic/contacts APIs used, so nothing else is required. No ATT prompt needed (no AdSupport, no `ATTrackingManager`).
- **iPad risk avoided** — `TARGETED_DEVICE_FAMILY = 1` in both configs (`project.pbxproj:683`, `:713`), portrait-only, iPhone-only. Screenshots at 1290×2796 fit the 6.9" slot.
- **No deprecated/private APIs** across all 68 Swift files (no UIWebView, UIAlertView, `unarchiveObject`, KVC `valueForKey`); deployment target 17.0, Swift 5.9; web content via `SFSafariViewController`.
- **4.2 / 4.3(b)** — substantial native SwiftUI surface (Studio wizard, Journal, Style DNA, calibration), not a wrapper or a single model call. No remote config or kill-switch (`FeatureGates.swift` reads only the local RevenueCat entitlement).
- **Server-side guardrails on AI output**: `analyze-body/index.ts:9`, `:78-80`, `:102` forbid weight/BMI/age/height estimation with an allowlist-validated schema (`:41-46`, `:151-159`); `stylist-chat/index.ts:27`, `:32` ban personal attacks; `score-outfit/index.ts:14-16`, `:165` coerce tone to honest. RLS is on for user tables (`0003_magazine_covers.sql:11-14`), photos fetched via 1-hour signed URLs (`Repo.swift:266`).
- **In-app data export** exists alongside deletion (`You/HelpPrivacySheet.swift:69-73`, `:117-133`), backing the GDPR portability claim at `web/app/privacy/page.tsx:144-151`.

---

## Not an issue (investigated and cleared)

- **"Brutal mode is still live and monetized on iOS"** — false. `honestySection` (`YouSheet.swift:178`) has zero callers, `FeatureGates.requireBrutal()` has zero callers, `CreditsSheet.swift:83` suppresses the brutal paywall hero, and `grep -rn "request(.roast)" ios/` returns nothing. Only dead code remains.
- **"iOS has no content-reporting mechanism → 1.2 blocker"** — the fact is true but 1.2 governs multi-user surfaces; Fitrater has no feed, comments, or cross-account sharing (`REVIEW_NOTES.md:9`). The Android report sheet was built for **Google Play's** GenAI policy (`0006_content_reports.sql:2-4` says so). Downgraded to nice-to-have.
- **"Credit-pack product IDs don't exist in ASC / packs are unbuyable"** — backwards. `SESSION_2026-08-03.md:22-27` records all four `fitrater_v2_credits_*` records as READY_TO_SUBMIT with ASC ids; the `_pro_credits_` ids in the docs and `.storekit` are the deleted false start.
- **"Credit packs aren't in the RevenueCat Offering"** — rests on `REVENUECAT_SETUP.md`, which `SESSION_2026-08-03.md:105-108` supersedes. The on-device `_500`/`_5000` price-collision bug proves both packs resolved from `availablePackages`.
- **"Magazine covers land in a public bucket → 5.1.2 blocker"** — the bucket is public by design (Fal must fetch the template over the internet; users share the output), keys are two random UUIDs with no LIST policy, and bucket ACLs are invisible to App Review. Hygiene item, not a submission issue.
- **"Missing PrivacyInfo.xcprivacy is a blocker"** — no first-party required-reason API usage, dependencies ship their own manifests, and builds 14/15 already cleared ASC processing.
- **IDFA** — `google-ads-on-device-conversion-ios-sdk` is a transitive resolution artifact, not a linked product; no AdSupport/ATT usage. `APP_STORE_SUBMISSION_CHECKLIST.md:104` "IDFA: No" is correct.
- **"iOS is behind Android / uncommitted work threatens the iOS build"** — inverted. `git status --porcelain` shows 26 modified + untracked paths and **zero under `ios/`**; the pending work is an Android catch-up port of features iOS shipped in `d903268`.
- **"The four AI edge functions are missing"** — all present: `compare-outfits`, `decode-outfit`, `generate-piece`, `occasion-coach` exist and every iOS call site has a matching directory.

---

## iOS vs Android parity

Uncommitted work in this branch is an **Android catch-up port**, not an iOS regression — `git status` shows no modified iOS files. Three things exist on Android that iOS lacks:

1. **Content reporting** — `ReportContentSheet.kt` (untracked) wired into 5 Android surfaces, writing to `content_reports` (`Repo.kt:272`). iOS: zero hits for `content_reports` in `ios/`. **Does not block iOS submission** (no user-to-user surface, and it was built for Play's GenAI policy), but it is the cheapest hedge if a reviewer improvises a moderation ask against Stylist Chat.
2. **Roast deletion** — Android staged `D .../camera/RoastScreen.kt` with no dangling references. iOS still compiles `Camera/RoastView.swift` as unreachable dead code. **Does not block** — cleanup only.
3. **Correct entitlement id** — Android uses `fitscore_pro` correctly (`data/billing/RcBilling.kt:41`); iOS `ManageProView.swift:11` uses `"pro"`. iOS-only bug, listed above.

Nothing on iOS is blocked *by* Android. The shared backend (`supabase/migrations/0006`, the un-deployed edge functions) is the only coupling.

---

## Submission checklist — in order

**Phase 1 — unblock the upload (do first, everything else is wasted otherwise)**

1. Change `ios/FitScore/FitScore/Info.plist:12-13` to `<string>$(MARKETING_VERSION)</string>` and `<string>$(CURRENT_PROJECT_VERSION)</string>`. Bump `CURRENT_PROJECT_VERSION` to **16** in both configs (`project.pbxproj:669`, `:699`) — build 4 has already been consumed.
2. Archive once and confirm in Organizer (or `plutil -p "$ARCHIVE/Products/Applications/Fitrater.app/Info.plist"`) that it reads **1.0 (16)**. Add that assertion to `ios/store/submit.sh`.
3. Remove `CODE_SIGN_IDENTITY = "Apple Development"` from the Release config (`project.pbxproj:698`), set `ENABLE_PREVIEWS = NO` for Release, and delete `aps-environment` from `Fitrater.entitlements` (also disable Push on the App ID).

**Phase 2 — code fixes that require a new binary**

4. Add `supabase/functions/delete-account` (service-role `auth.admin.deleteUser` + Apple token revocation + `magazine_covers` storage sweep); call it from `Repo.deleteAllUserData()` before `signOut()`; stop swallowing errors with `try?`.
5. Fix `ManageProView.swift:11` → `RcBilling.entitlementPro`.
6. Guard the Stylist Chat canned fallback behind `#if DEBUG` (`MainTabView.swift:258-262`) and surface real errors; wire or remove the inert chips (`:453-466`).
7. Derive the trial disclosure from `introductoryDiscount` per package (`CreditsSheet.swift:180-192`); post a toast in the purchase catch (`:283-285`).
8. Add a Terms/Privacy footer to `SignInView.swift`; add a Restore row to `ManageProView` (or correct `REVIEW_NOTES.md:47`); fix the support address at `HelpPrivacySheet.swift:84`.
9. Add the one-time face-swap consent confirmation in `MagazineCoverSheet.swift`.
10. Delete `Onboarding/HemIntroView.swift`, `Camera/RoastView.swift`, the `.roast` enum case and switch arm, `requireBrutal()`, and the unused `transcribeIntent`.
11. Rewrite the brand-name option subtitles in `CoverTemplateCreatorView.swift:6-13, :17, :39, :64, :474`; either drop the Istanbul weather card or wire real location + `NSLocationWhenInUseUsageDescription`.

**Phase 3 — backend + web (no rebuild needed)**

12. Deploy `stylist-chat`, `compose-cover`, `score-outfit`; push `0005_stylist_memory` and `0006_content_reports`; `git add supabase/migrations/0006_content_reports.sql`. Verify each returns 200 with the demo account.
13. Ship `web/app/support/page.tsx` and confirm `curl https://fitrater.ai/support` → 200.
14. Fix `web/app/terms/page.tsx:28` ("roasts") and `:47-49` ("in-app rewards"); fix `web/app/paywall/page.tsx:43-67` prices, dead Restore button, and "FitScore Pro"; delete or Android-scope the location paragraph at `web/app/privacy/page.tsx:48-53`; add Google/Firebase to `web/app/privacy/page.tsx` §5.
15. Revoke Supabase PAT `sbp_<redacted-revoke-this>`, move `/Users/efe/clothes31/file` out of the repo, rotate the Android keystore passwords.

**Phase 4 — metadata and assets**

16. Provision `applereview+demo@fitrater.ai` in Supabase **with an explicit password set server-side** (in-app signup is magic-link only), grant Pro + credits, pre-seed a calibration photo. Sign in on a clean install via "Have a password?" and run one Try-on and one Compose Cover end to end.
17. Rewrite `ios/store/REVIEW_NOTES.md` completely: real password, real phone number (`:68`), correct RC key `appl_XcRn…` (`:35`), correct `fitrater_v2_credits_*` SKUs and prices (`:43-45`), delete "brutal-mode critiques" (`:15`), delete the false moderation-pass sentence (`:64`), correct the Restore claim (`:47`), correct the deletion claim (`:59`), and add a walkthrough covering Chat with Hem, A vs B, Decode style, Occasion Coach, Decode the invite, the Studio wizard, and body calibration.
18. Delete "brutal-mode critiques" from `APP_STORE_METADATA.md:54` and `APP_STORE_IN_APP_PURCHASES.md:35`, `:41`. Remove `vogue` from `APP_STORE_METADATA.md:61`.
19. Re-capture all six screenshots natively at 1290×2796 from the new build; delete `2_studio.png` (Vogue reproduction); fix the duplicate `5_generate.png`/`6_tryon.png` and the mislabeled `3_cover.png`.
20. Rewrite `ios/store/APP_PRIVACY_ANSWERS.md` to reflect Firebase (or remove Firebase from the target and leave it as-is).

**Phase 5 — App Store Connect**

21. Upload the build; confirm it appears as **1.0 (16)**, not (4).
22. **App Privacy → Edit**: enter the corrected answers — Identifiers→Device ID, Usage Data→Product Interaction, Diagnostics→Crash Data (third-party: Google), plus the existing Email/Photos/User Content/Purchase History. IDFA: No. Tracking: No.
23. **Age Rating questionnaire**: re-run under the current tiers. Disclose the AI chatbot (in-app messaging, no user-to-user contact) and AI-generated imagery. Do **not** answer "all None". Pick 13+ or 16+ and make `web/app/terms` agree.
24. **In-App Purchases**: confirm all three subs + four `fitrater_v2_credits_*` consumables are Ready to Submit and **explicitly attached to this version's submission** (`SESSION_2026-08-03.md:126-133` flags that the API would not attach them — verify in the UI; if Apple does not auto-bundle, the paywall errors during review).
25. **App Review Information**: sign-in required = Yes, paste the real demo email + password + phone, paste the rewritten review notes.
26. **App Information**: Support URL → the now-live `/support` (or `https://fitrater.ai`); Privacy Policy URL → `/privacy`; description/keywords/promotional text from the corrected `APP_STORE_METADATA.md`.
27. **Export compliance**: nothing to do — already declared in the plist.
28. Final smoke test on a device with the demo account: sign in → calibration → score → try-on → cover → chat → purchase in sandbox → Restore → Manage Pro (should now show renewal date) → Delete account → attempt to sign back in with the same email (**must fail**). Then Submit for Review.